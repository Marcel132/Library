// import variables from DOM
const navigation = document.querySelector('.navigation') // Navigation element
const books = document.querySelector('.books') // bBooks element
const footer = document.querySelector('.footer') // Footer element

const logInForm = document.getElementById("log-in") // Login form
const logInBtn = document.getElementById("log-in-btn") //Login button

const signUpForm = document.getElementById("sign-up") // Sign up form
const signUpBtn = document.getElementById("sign-up-btn") //Sign up button

const libraryDiv = document.getElementById('library') // Library form
const addBookBtn = document.getElementById('add-book-btn') // Library button

const profileButton = document.getElementById('profile-button') // Profile button
const account = document.getElementById('account') // Account button